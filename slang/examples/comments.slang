(*
    allow multi-line comments
    (* as well as (*nested comments*)*)
*)

// single line comments
// allow arbitrary characters after '//' until a newline

(fun (x : int) -> fun (y :int) -> x + y end end) 2 3 // single line comments can also follow expressions